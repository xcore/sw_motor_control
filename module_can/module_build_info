CAN_SOURCE_DIRS = src

SOURCE_DIRS += $(CAN_SOURCE_DIRS)

INCLUDE_DIRS += $(CAN_SOURCE_DIRS)

EXCLUDE_FILES += 

$(call SET_XCC_XC_FLAGS, $(CAN_SOURCE_DIRS), $(XCC_FLAGS) -g -O3)
$(call SET_XCC_C_FLAGS, $(CAN_SOURCE_DIRS), $(XCC_FLAGS) -O3)
